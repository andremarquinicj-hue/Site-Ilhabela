// Para um projeto Firebase exclusivo desta viagem.
// O site acessa os dados apenas pela API da Vercel, autenticada com conta de serviço.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} { allow read, write: if false; }
  }
}
